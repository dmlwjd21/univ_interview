{
  "name": "univ-interview-archive",
  "version": "2.0.0",
  "private": true,
  "engines": { "node": "22.x" },
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local scripts/dev-server.js",
    "build": "node scripts/build.js",
    "check": "node scripts/check.js",
    "test": "node --test tests/search.test.js"
  }
}
